{
  "name": "scratch-meow",
  "version": "1.0.0",
  "description": "A react playground for VS Codespaces on github to try out some snippets",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/app.tsx --bundle --minify --outdir=www/js",
    "dev": "esbuild src/app.tsx --bundle --minify --outdir=www/js --servedir=www/ --serve"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/react": "^18.2.14",
    "@types/react-dom": "^18.2.6",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.18.11"
  }
}
